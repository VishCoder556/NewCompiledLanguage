ptr<int> malloc(int size);
void exit(int return);
void printf(string fmt, ...)

struct Hello {
    int a
}

int main(int argc, ptr<string> argv){
    struct Hello hello;
    int i = 0;
    while (i < argc){
        printf("Argument #%d: %s\n", i, argv[i])
        i = i + 1
    }
    return 0;
}
