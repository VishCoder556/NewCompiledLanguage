#!include "examples/include/std.lang"

struct Vector {
    int x
    char y
    int z
}

int main() {

    struct Vector v
    v.x = 25
    v.y = cast(char, 200)
    v.z = 100
    
    v.y = cast(char, v.x + 40)
    printf("%d, %c, %d\n", v.x, v.y, v.z);
    return 0;
}
