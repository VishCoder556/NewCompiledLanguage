#!include "examples/include/std.lang"

struct Hello {
    int a
    int b
}

int main(int argc, ptr<string> argv){
    ptr<struct Hello> h = cast(ptr<struct Hello>, malloc(16));
    deref(h).b = 5
    return deref(h + cast(ptr<struct Hello>, 8));
}
