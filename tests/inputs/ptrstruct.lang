ptr<int> malloc(int size);
void exit(int return);
void printf(string fmt, ...)

struct Hello {
    int a
    int b
}

int main(int argc, ptr<string> argv){
    ptr<struct Hello> h = cast(ptr<struct Hello>, malloc(16));
    deref(h).b = 5
    printf("%d\n", deref(h + cast(ptr<struct Hello>, 8)))
    return 0;
}
